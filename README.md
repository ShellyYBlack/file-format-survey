# File format survey
This script supports reporting of file format types in SCRC's born-digital collections. It that combines Siegfried and FITS reports into a Dataframe, then calculates the total number of file formats. Results can be output to a CSV or data visualization. 
